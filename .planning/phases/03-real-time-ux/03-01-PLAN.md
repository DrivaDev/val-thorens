---
phase: 03-real-time-ux
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/page.tsx
autonomous: true
requirements: [PROG-01, PROG-02, PROG-03, PROG-04, PROG-05]

must_haves:
  truths:
    - "El usuario ve un log en vivo que agrega una línea por cada evento SSE en tiempo real, sin recargar la página"
    - "Cada tipo de evento se renderiza con su color: verde=sent, rojo=send_error, gris=scraping con email null, azul=resto"
    - "El log tiene altura fija con scroll y hace auto-scroll a la última línea"
    - "Al llegar el evento complete, el log desaparece y se muestra una tarjeta de resumen con emails enviados, empleadores omitidos y 'Registrado en Google Sheets'"
    - "El usuario puede hacer click en 'Volver al formulario' y vuelve a ver el formulario"
  artifacts:
    - path: "app/page.tsx"
      provides: "Componente ProgressView (consumidor SSE + log + resumen) y estado 'progress' en Home"
      contains: "function ProgressView"
  key_links:
    - from: "FormView.handleSubmit"
      to: "Home (estado progress)"
      via: "onSubmitStart(response) pasa el Response del fetch"
      pattern: "onSubmitStart"
    - from: "ProgressView"
      to: "response.body.getReader()"
      via: "lectura del stream en useEffect al montar"
      pattern: "getReader\\(\\)"
    - from: "ProgressView"
      to: "Home (estado form)"
      via: "onReset()"
      pattern: "onReset"
---

<objective>
Reemplazar la vista estática `LoadingView` (spinner) en `app/page.tsx` por una vista `ProgressView` que consume el stream SSE de `/api/run` en tiempo real, renderiza cada evento como una línea de log con color, y al finalizar reemplaza el log por una tarjeta de resumen.

Purpose: Cumplir PROG-01 a PROG-05 — el usuario debe ver el progreso del pipeline en vivo y un resumen claro al final, con opción de reiniciar.
Output: `app/page.tsx` modificado con: (1) plumbing del estado `progress` en `Home`, (2) `FormView.handleSubmit` que captura el `Response` y lo pasa hacia arriba, (3) nuevo componente `ProgressView` con consumidor SSE, log con auto-scroll, tarjeta de resumen y botón de reset.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-real-time-ux/03-CONTEXT.md
@app/page.tsx
@app/api/run/route.ts

<interfaces>
<!-- Tipos de evento SSE emitidos por app/api/run/route.ts. El executor DEBE usar estos exactos. -->
<!-- Formato del stream: cada evento llega como una línea "data: {json}\n\n" -->

Eventos emitidos (campo `type`):
- { type: 'searching', message: string }                          → línea azul
- { type: 'discovery_complete', total: number }                   → línea azul
- { type: 'scraping', employer: string, email: string | null }    → gris si email===null, azul si email !== null
- { type: 'generating', employer: string }                        → línea azul
- { type: 'sent', employer: string, email: string }               → línea verde
- { type: 'logged', employer: string }                            → línea azul
- { type: 'send_error', employer: string, error: string }         → línea roja
- { type: 'complete', sent: number, skipped: number }             → dispara el reemplazo por la tarjeta de resumen

Nota: el evento `send_error` con employer === 'pipeline' representa un fallo fatal del discovery; igual le sigue un `complete` con sent/skipped. No requiere manejo especial — se muestra como línea roja normal (D-08, D-10).

Componentes reutilizables existentes en app/page.tsx:
- function Card({ children, className })          línea ~265 — Card con franja tricolor
- function FlyingBackground()                     línea ~137 — fondo animado
- Loader2 (lucide-react)                          ya importado, línea 5
- CheckCircle2 (lucide-react)                      ya importado, línea 5
- Colores Tailwind: text-french-blue, text-french-red, bg-french-blue, border-french-red (configurados)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Plumbing del estado 'progress' en Home y captura del Response en FormView</name>
  <files>app/page.tsx</files>
  <read_first>
    - app/page.tsx (estado actual de Home líneas 717-735, FormView líneas 363-711, especialmente handleSubmit líneas 429-458)
    - app/api/run/route.ts (confirmar que POST devuelve un Response con stream text/event-stream)
    - .planning/phases/03-real-time-ux/03-CONTEXT.md (decisiones D-07, D-09)
  </read_first>
  <action>
    Modificar el componente `Home` (líneas 717-735) y `FormView` (líneas 363-458):

    1. En `Home`, cambiar el tipo del estado `view` de `"login" | "form" | "loading"` a `"login" | "form" | "progress"`.
       Agregar un nuevo estado: `const [runResponse, setRunResponse] = useState<Response | null>(null);`

    2. En `Home`, dentro del bloque `if (status === "authenticated")`, reemplazar la lógica actual:
       - Eliminar la rama `if (view === "loading") return <LoadingView />;`
       - Agregar: `if (view === "progress" && runResponse) return <ProgressView response={runResponse} onReset={() => { setRunResponse(null); setView("form"); }} />;`
       - Mantener el return final: `return <FormView session={session} onSubmitStart={(res) => { setRunResponse(res); setView("progress"); }} />;`

    3. En `FormView`, cambiar la firma de props: reemplazar `onSubmitComplete: () => void;` por `onSubmitStart: (response: Response) => void;` (tanto en el tipo del parámetro como en la desestructuración).

    4. En `FormView.handleSubmit` (líneas 429-458), cambiar la lógica de fetch para capturar y propagar el Response:
       - Hacer `handleSubmit` `await` el fetch en lugar de descartarlo con `.catch(() => {})`.
       - Reemplazar el bloque actual por:
         ```ts
         setIsSubmitting(true);
         try {
           const response = await fetch("/api/run", {
             method: "POST",
             headers: { "Content-Type": "application/json" },
             body: JSON.stringify({
               name: formData.name,
               cvBase64: formData.cvBase64,
               jobTypes: formData.jobTypes,
               languages: formData.languages,
               availFrom: formData.availFrom,
               availTo: formData.availTo,
               hasEUPassport: formData.hasEUPassport,
               accessToken: session.access_token,
             }),
           });
           if (!response.ok || !response.body) {
             throw new Error("El servidor rechazó la solicitud");
           }
           onSubmitStart(response);
         } catch {
           setErrors((e) => ({
             ...e,
             submit: "Error al iniciar el proceso. Intentalo de nuevo.",
           }));
           setIsSubmitting(false);
         }
         ```

    5. NO eliminar la función `LoadingView` todavía — quedará sin usar tras esta tarea pero `ProgressView` la reemplaza en Task 2. (Si prefieres, déjala; será removida o reemplazada en Task 2/3 — no es bloqueante.)

    Nota: `ProgressView` aún no existe — el código compilará con error de referencia hasta Task 2. Esto es esperado dentro del mismo plan; el executor debe completar Task 2 antes de verificar el build.
  </action>
  <verify>
    <automated>grep -n "view === \"progress\"" app/page.tsx && grep -n "onSubmitStart" app/page.tsx && grep -n "runResponse" app/page.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "onSubmitStart" app/page.tsx` retorna al menos 3 (tipo de prop, desestructuración, llamada en Home)
    - `grep -n "useState<Response | null>(null)" app/page.tsx` encuentra la declaración de runResponse
    - `grep -n "\"login\" | \"form\" | \"progress\"" app/page.tsx` encuentra el tipo de estado actualizado
    - `grep -n "const response = await fetch(\"/api/run\"" app/page.tsx` confirma que el fetch ahora se await y captura
    - `grep -c "onSubmitComplete" app/page.tsx` retorna 0 (la prop vieja fue eliminada por completo)
    - `grep -n "view === \"loading\"" app/page.tsx` retorna 0 (la rama loading fue eliminada)
  </acceptance_criteria>
  <done>Home tiene estado 'progress' + runResponse; FormView captura el Response del fetch y lo pasa vía onSubmitStart; la prop onSubmitComplete y la rama "loading" ya no existen.</done>
</task>

<task type="auto">
  <name>Task 2: Componente ProgressView con consumidor SSE y log en vivo con auto-scroll</name>
  <files>app/page.tsx</files>
  <read_first>
    - app/page.tsx (componentes Card línea ~265, FlyingBackground línea ~137, imports línea 1-8; y los cambios de Task 1 en Home/FormView)
    - app/api/run/route.ts (función sseEvent línea 11-13 y todos los eventos enqueue del ReadableStream líneas 113-190 para confirmar tipos exactos)
    - .planning/phases/03-real-time-ux/03-CONTEXT.md (decisiones D-01, D-02, D-03, D-04)
  </read_first>
  <action>
    Crear el componente `ProgressView` en `app/page.tsx`, ubicado en la sección "Sub-views" (después de `FormView`, antes del comentario "Main" en la línea ~713).

    1. Definir el tipo de evento SSE arriba del componente (junto a las otras interfaces, sección líneas ~222-244):
       ```ts
       type SSEEvent =
         | { type: "searching"; message: string }
         | { type: "discovery_complete"; total: number }
         | { type: "scraping"; employer: string; email: string | null }
         | { type: "generating"; employer: string }
         | { type: "sent"; employer: string; email: string }
         | { type: "logged"; employer: string }
         | { type: "send_error"; employer: string; error: string }
         | { type: "complete"; sent: number; skipped: number };

       interface LogLine {
         text: string;
         color: "blue" | "green" | "red" | "gray";
       }
       ```

    2. Crear una función helper `eventToLogLine(ev: SSEEvent): LogLine | null` que mapea cada evento a una línea (retorna `null` para `complete`, que no es una línea de log). Textos en español:
       - searching → { text: ev.message, color: "blue" }
       - discovery_complete → { text: `Se encontraron ${ev.total} empleadores`, color: "blue" }
       - scraping con email === null → { text: `${ev.employer}: sin email, omitido`, color: "gray" }
       - scraping con email !== null → { text: `${ev.employer}: email encontrado (${ev.email})`, color: "blue" }
       - generating → { text: `Generando email para ${ev.employer}...`, color: "blue" }
       - sent → { text: `Email enviado a ${ev.employer}`, color: "green" }
       - logged → { text: `${ev.employer} registrado en Google Sheets`, color: "blue" }
       - send_error → { text: `Error con ${ev.employer}: ${ev.error}`, color: "red" }
       - complete → return null

    3. Definir el componente:
       ```tsx
       function ProgressView({
         response,
         onReset,
       }: {
         response: Response;
         onReset: () => void;
       }) {
         const [logLines, setLogLines] = useState<LogLine[]>([]);
         const [summary, setSummary] = useState<{ sent: number; skipped: number } | null>(null);
         const logEndRef = useRef<HTMLDivElement>(null);

         // Consumir el stream SSE al montar
         useEffect(() => {
           let cancelled = false;
           const reader = response.body!.getReader();
           const decoder = new TextDecoder();
           let buffer = "";

           async function read() {
             while (true) {
               const { done, value } = await reader.read();
               if (done || cancelled) break;
               buffer += decoder.decode(value, { stream: true });
               const parts = buffer.split("\n\n");
               buffer = parts.pop() ?? "";
               for (const part of parts) {
                 const line = part.trim();
                 if (!line.startsWith("data: ")) continue;
                 let ev: SSEEvent;
                 try {
                   ev = JSON.parse(line.slice(6));
                 } catch {
                   continue;
                 }
                 if (ev.type === "complete") {
                   setSummary({ sent: ev.sent, skipped: ev.skipped });
                 } else {
                   const logLine = eventToLogLine(ev);
                   if (logLine) setLogLines((prev) => [...prev, logLine]);
                 }
               }
             }
           }
           read().catch(() => {
             // Stream interrumpido — el evento complete normalmente cubre el cierre.
           });
           return () => {
             cancelled = true;
             reader.cancel().catch(() => {});
           };
           // eslint-disable-next-line react-hooks/exhaustive-deps
         }, []);

         // Auto-scroll al final cuando llega una nueva línea (D-03)
         useEffect(() => {
           logEndRef.current?.scrollIntoView({ behavior: "smooth" });
         }, [logLines.length]);

         // ... render (ver punto 4)
       }
       ```

    4. Render del componente. Usar el mismo wrapper de fondo que las otras vistas + `FlyingBackground` + `Card`. Mientras `summary === null`, mostrar el log:
       - Wrapper externo: `<div className="min-h-screen bg-gradient-to-br from-[#0a1929] via-[#0d2d5e] to-[#0055A4] flex items-center justify-center py-12 overflow-hidden">` con `<FlyingBackground />` y un `<div className="relative z-10 max-w-md w-full mx-4">` que contiene un `<Card>`.
       - Dentro del Card, header fijo (D-04): título `The Annex` (`font-display text-2xl text-french-blue`) y debajo, con `Loader2` girando, el texto fijo `"Proceso en curso..."` (`<div className="flex items-center gap-2 mt-2"><Loader2 className="w-5 h-5 animate-spin text-french-blue" /><p className="text-lg font-semibold text-gray-800">Proceso en curso...</p></div>`). NO mostrar contador dinámico.
       - Área de log (D-01, D-03): `<div className="mt-4 max-h-80 overflow-y-auto rounded-xl bg-gray-50 border border-gray-200 p-3 flex flex-col gap-1">` que mapea `logLines`. Cada línea:
         ```tsx
         {logLines.map((l, i) => (
           <p key={i} className={`text-sm leading-snug ${
             l.color === "green" ? "text-green-600"
             : l.color === "red" ? "text-french-red"
             : l.color === "gray" ? "text-gray-400"
             : "text-french-blue"
           }`}>
             {l.text}
           </p>
         ))}
         <div ref={logEndRef} />
         ```
       - El bloque de resumen (cuando `summary !== null`) se implementa en Task 3; por ahora dejar un placeholder mínimo `{summary && <div data-summary-placeholder />}` que Task 3 reemplaza, O implementar directamente si avanzas — pero el contrato de Task 3 es la tarjeta completa.

    Mantener todos los textos visibles en español.
  </action>
  <verify>
    <automated>grep -n "function ProgressView" app/page.tsx && grep -n "getReader()" app/page.tsx && grep -n "scrollIntoView" app/page.tsx && grep -n "max-h-80 overflow-y-auto" app/page.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "function ProgressView" app/page.tsx` encuentra la definición del componente
    - `grep -n "response.body!.getReader()" app/page.tsx` confirma la lectura del stream
    - `grep -n "new TextDecoder()" app/page.tsx` confirma el decoder
    - `grep -n "JSON.parse(line.slice(6))" app/page.tsx` confirma el parseo de líneas "data: "
    - `grep -n "scrollIntoView" app/page.tsx` confirma el auto-scroll (D-03)
    - `grep -n "max-h-80 overflow-y-auto" app/page.tsx` confirma el área de log con altura fija y scroll (D-03)
    - `grep -n "Proceso en curso..." app/page.tsx` confirma el header fijo (D-04)
    - `grep -c "text-green-600\|text-french-red\|text-gray-400\|text-french-blue" app/page.tsx` confirma que los 4 colores de línea están presentes (D-02)
    - `grep -n "type SSEEvent" app/page.tsx` confirma el tipo de evento definido
  </acceptance_criteria>
  <done>ProgressView lee el stream SSE de la response, parsea cada evento, agrega líneas coloreadas al log con auto-scroll, y muestra el header fijo "Proceso en curso..." dentro de un Card con FlyingBackground. El evento complete guarda el summary en estado.</done>
</task>

<task type="auto">
  <name>Task 3: Tarjeta de resumen final, botón "Volver al formulario" y verificación de build</name>
  <files>app/page.tsx</files>
  <read_first>
    - app/page.tsx (componente ProgressView creado en Task 2, especialmente el render y el estado summary; componente Card línea ~265)
    - .planning/phases/03-real-time-ux/03-CONTEXT.md (decisiones D-05, D-06, D-07, D-08)
  </read_first>
  <action>
    Completar el render de `ProgressView` para que, cuando `summary !== null`, el log sea REEMPLAZADO por la tarjeta de resumen (D-05: el log desaparece).

    1. En el render de `ProgressView`, envolver la lógica condicionalmente: si `summary !== null`, renderizar SOLO la tarjeta de resumen (no el header de progreso ni el log). Si `summary === null`, renderizar el header + log (de Task 2).

    2. Contenido de la tarjeta de resumen (dentro del mismo wrapper de fondo + FlyingBackground + Card), textos en español (D-06):
       ```tsx
       <div className="text-center">
         <CheckCircle2 className="text-green-600 w-12 h-12 mx-auto" />
         <h2 className="font-display text-2xl text-french-blue mt-3">Proceso completado</h2>
         <div className="flex justify-center gap-8 mt-6">
           <div>
             <p className="text-3xl font-bold text-green-600">{summary.sent}</p>
             <p className="text-sm text-gray-500 mt-1">emails enviados</p>
           </div>
           <div>
             <p className="text-3xl font-bold text-gray-400">{summary.skipped}</p>
             <p className="text-sm text-gray-500 mt-1">empleadores omitidos</p>
           </div>
         </div>
         <p className="text-sm text-gray-500 mt-6 flex items-center justify-center gap-2">
           <CheckCircle2 className="w-4 h-4 text-green-600" />
           Registrado en Google Sheets
         </p>
         <button
           onClick={onReset}
           className="w-full bg-french-blue text-white rounded-xl py-3 px-6 text-base font-bold hover:bg-blue-800 active:scale-[0.98] transition-all duration-200 mt-8 shadow-lg shadow-blue-200"
         >
           Volver al formulario
         </button>
       </div>
       ```
       El texto del botón debe ser exactamente `Volver al formulario` (D-07) y debe llamar a `onReset` (PROG-05).
       La confirmación de Sheets es el texto fijo `Registrado en Google Sheets` (D-06).
       Nota (D-08): si el pipeline falla fatalmente, `complete` llega con sent=0/skipped=0 y esta misma tarjeta muestra "0 emails enviados / 0 empleadores omitidos" — no se necesita pantalla de error separada.

    3. Eliminar la función `LoadingView` (líneas ~342-361) ya que fue reemplazada por `ProgressView` y ya no se referencia en ningún lado.

    4. Verificar que el proyecto compila sin errores de TypeScript ni lint: ejecutar el build.
  </action>
  <verify>
    <automated>grep -n "Volver al formulario" app/page.tsx && grep -n "Registrado en Google Sheets" app/page.tsx && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "Volver al formulario" app/page.tsx` encuentra el texto EXACTO del botón (D-07, PROG-05)
    - `grep -n "onClick={onReset}" app/page.tsx` confirma que el botón llama onReset
    - `grep -n "Registrado en Google Sheets" app/page.tsx` confirma la confirmación de Sheets (D-06)
    - `grep -n "summary.sent" app/page.tsx` y `grep -n "summary.skipped" app/page.tsx` confirman que se muestran ambos contadores (D-06)
    - `grep -c "function LoadingView" app/page.tsx` retorna 0 (LoadingView eliminado)
    - `npm run build` termina con exit code 0 (compila sin errores de TypeScript ni referencias rotas)
  </acceptance_criteria>
  <done>La tarjeta de resumen reemplaza el log al recibir complete, muestra emails enviados + empleadores omitidos + "Registrado en Google Sheets", y el botón "Volver al formulario" llama onReset. LoadingView eliminado. El build pasa limpio.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| servidor SSE → cliente (ProgressView) | datos del stream `/api/run` parseados por el cliente como JSON |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Tampering | parseo de eventos SSE en ProgressView | mitigate | `JSON.parse` envuelto en try/catch — líneas malformadas se ignoran (`continue`), nunca crashean el render |
| T-03-02 | Denial of Service | stream sin cerrar al desmontar ProgressView | mitigate | cleanup del useEffect llama `reader.cancel()` y setea flag `cancelled` para detener el loop de lectura |
| T-03-03 | Information Disclosure | texto de error de `send_error` mostrado en el log | accept | el error proviene del propio pipeline del usuario sobre sus propios envíos; no hay datos de terceros sensibles, app en modo Testing para amigos |
</threat_model>

<verification>
- `npm run build` pasa sin errores (compila TypeScript + lint de Next.js)
- ProgressView consume el stream: contiene `getReader()`, `TextDecoder`, `JSON.parse(line.slice(6))`
- Las 8 categorías de eventos están mapeadas en `eventToLogLine` (searching, discovery_complete, scraping null/non-null, generating, sent, logged, send_error) — PROG-02
- Log con `max-h-80 overflow-y-auto` + `scrollIntoView` — PROG-03
- Tarjeta de resumen con sent/skipped/"Registrado en Google Sheets" — PROG-04
- Botón exacto "Volver al formulario" llamando onReset — PROG-05
- El stream SSE ya existía desde Phase 2 (`/api/run`) — PROG-01 se cumple al consumirlo desde el frontend
</verification>

<success_criteria>
1. El frontend muestra un log en vivo con scroll mientras el pipeline corre; los eventos aparecen en tiempo real sin recargar (PROG-01, PROG-03)
2. Cada evento SSE se renderiza con su color correcto cubriendo todo el espectro de estados (PROG-02)
3. Al finalizar, una tarjeta de resumen reemplaza el log mostrando enviados, omitidos y confirmación de Sheets (PROG-04)
4. El usuario puede hacer click en "Volver al formulario" para reiniciar (PROG-05)
5. `npm run build` pasa limpio
</success_criteria>

<output>
After completion, create `.planning/phases/03-real-time-ux/03-01-SUMMARY.md`
</output>
