---
phase: 02-pipeline
plan: "03"
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - lib/gemini.ts
  - lib/gmail.ts
autonomous: true
requirements: [GEN-01, GEN-02, GEN-03, GEN-04, SEND-01, SEND-02, SEND-03, SEND-04]

must_haves:
  truths:
    - "generateEmailBody() llama Gemini 2.0 Flash con un prompt en francés que incluye intro del candidato, interés en el establecimiento, disponibilidad, idiomas, mención de CV adjunto, y cierre cordial"
    - "Los errores 429 de Gemini disparan retry exponencial: espera 1s, 2s, 4s con máximo 3 intentos antes de lanzar el error"
    - "sendEmail() construye un mensaje MIME multipart/mixed con body texto y adjunto PDF codificado en base64"
    - "La codificación base64url aplica los 3 reemplazos obligatorios: + → -, / → _, = eliminado"
    - "sendEmail() usa el access_token OAuth del usuario para autenticar con Gmail API"
  artifacts:
    - path: "lib/gemini.ts"
      provides: "Función generateEmailBody() con retry exponencial para 429"
      exports: ["generateEmailBody", "CandidateData"]
    - path: "lib/gmail.ts"
      provides: "Función sendEmail() con MIME multipart y base64url"
      exports: ["sendEmail"]
  key_links:
    - from: "lib/gemini.ts"
      to: "generativelanguage.googleapis.com"
      via: "GoogleGenerativeAI SDK — model.generateContent()"
      pattern: "generateContent"
    - from: "lib/gmail.ts"
      to: "gmail.googleapis.com/gmail/v1/users/me/messages/send"
      via: "fetch POST con Authorization: Bearer accessToken"
      pattern: "gmail\\.googleapis\\.com/gmail/v1/users/me/messages/send"
---

<objective>
Implementar los dos módulos de generación y envío de emails: `lib/gemini.ts` que genera el cuerpo del email personalizado en francés via Gemini 2.0 Flash con retry exponencial, y `lib/gmail.ts` que construye el mensaje MIME y lo envía via Gmail API con el access_token del usuario.

Purpose: Estos módulos implementan el núcleo de valor del producto — la personalización masiva de candidaturas con CV adjunto enviadas desde la cuenta propia del usuario.
Output: lib/gemini.ts exporta CandidateData y generateEmailBody(); lib/gmail.ts exporta sendEmail() con construcción MIME manual.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/02-pipeline/02-RESEARCH.md
@.planning/phases/02-pipeline/02-PATTERNS.md
@.planning/phases/02-pipeline/02-01-SUMMARY.md

<interfaces>
<!-- Contratos que este plan define y que run/route.ts (Wave 3) consumirá -->

Interfaz CandidateData que este plan define:
```typescript
export interface CandidateData {
  name: string;
  jobTypes: string[];
  languages: string;
  availFrom: string;
  availTo: string;
}
```

Contrato de generateEmailBody que run/route.ts consumirá:
```typescript
export async function generateEmailBody(
  candidate: CandidateData,
  employer: { name: string }
): Promise<string>
```

Contrato de sendEmail que run/route.ts consumirá:
```typescript
export async function sendEmail(params: {
  accessToken: string;
  to: string;
  subject: string;
  body: string;
  cvBase64: string;
  cvFilename: string;
  fromEmail: string;
}): Promise<void>
```

Desde app/page.tsx (lo que el frontend envía al pipeline — ya implementado en Fase 1):
```typescript
// handleSubmit en app/page.tsx envía:
fetch("/api/run", {
  method: "POST",
  body: JSON.stringify({
    name: formData.name,
    cvBase64: formData.cvBase64,
    jobTypes: formData.jobTypes,
    languages: formData.languages,
    availFrom: formData.availFrom,
    availTo: formData.availTo,
    accessToken: session.access_token,
  })
})
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: lib/gemini.ts — Generación de email en francés con retry exponencial para 429</name>
  <files>lib/gemini.ts</files>
  <read_first>
    - C:\Users\Equipo\Documents\val-thorens\.planning\phases\02-pipeline\02-RESEARCH.md (sección "Pattern 4: Gemini 2.0 Flash", líneas 420-489)
    - C:\Users\Equipo\Documents\val-thorens\.planning\phases\02-pipeline\02-PATTERNS.md (sección "lib/gemini.ts", líneas 273-322)
  </read_first>
  <action>
Crear `lib/gemini.ts` con el siguiente contenido exacto:

```typescript
import { GoogleGenerativeAI } from '@google/generative-ai';

const genAI = new GoogleGenerativeAI(process.env.GOOGLE_GEMINI_API_KEY!);

export interface CandidateData {
  name: string;
  jobTypes: string[];
  languages: string;
  availFrom: string;
  availTo: string;
}

export async function generateEmailBody(
  candidate: CandidateData,
  employer: { name: string }
): Promise<string> {
  const model = genAI.getGenerativeModel({ model: 'gemini-2.0-flash' });

  const prompt = `Escribe un email de candidatura en francés profesional y cordial para una temporada de invierno en Val Thorens.

Candidato: ${candidate.name}
Tipos de trabajo buscados: ${candidate.jobTypes.join(', ')}
Idiomas: ${candidate.languages}
Disponibilidad: del ${candidate.availFrom} al ${candidate.availTo}

Empleador: ${employer.name}

El email debe incluir obligatoriamente:
1. Presentación breve del candidato con su nombre
2. Interés específico en trabajar en el establecimiento "${employer.name}"
3. Disponibilidad exacta mencionada (del ${candidate.availFrom} al ${candidate.availTo})
4. Idiomas hablados: ${candidate.languages}
5. Mención explícita de que el CV se adjunta al email
6. Cierre cordial con el nombre del candidato

Responde ÚNICAMENTE con el cuerpo del email en francés, sin asunto, sin "Objet:", sin bloques de código, sin comillas alrededor del texto.`;

  return await callWithRetry(() => model.generateContent(prompt));
}

// GEN-04: retry exponencial para errores 429 (rate limit de Gemini API)
async function callWithRetry(
  fn: () => Promise<{ response: { text: () => string } }>,
  maxAttempts = 3
): Promise<string> {
  for (let attempt = 0; attempt < maxAttempts; attempt++) {
    try {
      const result = await fn();
      return result.response.text();
    } catch (error: unknown) {
      const err = error as { status?: number; message?: string };
      const is429 = err?.status === 429 || err?.message?.includes('429');

      if (is429 && attempt < maxAttempts - 1) {
        const delay = Math.pow(2, attempt) * 1000; // 1s, 2s, 4s
        console.warn(`[gemini] 429 recibido — reintentando en ${delay}ms (intento ${attempt + 1}/${maxAttempts})`);
        await new Promise((resolve) => setTimeout(resolve, delay));
        continue;
      }

      throw error; // re-throw si no es 429 o se agotaron los intentos
    }
  }
  throw new Error('[gemini] Max retries exceeded');
}
```

Notas críticas:
- Modelo: `gemini-2.0-flash` (CLAUDE.md lo especifica; deprecated pero funcional para v1)
- El prompt incluye TODOS los elementos requeridos por GEN-02: intro, interés en establecimiento, disponibilidad, idiomas, nota de CV adjunto, cierre
- GEN-03: El asunto del email NO se genera aquí — se hardcodea en run/route.ts como `Candidature - Saison d'hiver ${year} - ${name}`
- GEN-04: El retry solo aplica a errores 429; cualquier otro error se propaga al caller
- `Math.pow(2, attempt) * 1000` → attempt 0 = 1000ms, attempt 1 = 2000ms, attempt 2 = 4000ms
  </action>
  <verify>
    <automated>cd "C:/Users/Equipo/Documents/val-thorens" && node -e "const fs = require('fs'); const c = fs.readFileSync('lib/gemini.ts', 'utf8'); const checks = [['import GoogleGenerativeAI', \"from '@google/generative-ai'\"], ['GOOGLE_GEMINI_API_KEY', 'GOOGLE_GEMINI_API_KEY!'], ['gemini-2.0-flash', \"'gemini-2.0-flash'\"], ['export CandidateData', 'export interface CandidateData'], ['export generateEmailBody', 'export async function generateEmailBody'], ['prompt CV adjunto', 'CV se adjunta'], ['prompt disponibilidad', 'availFrom'], ['retry 429', '429'], ['Math.pow exponential', 'Math.pow(2, attempt)'], ['maxAttempts 3', 'maxAttempts = 3']]; checks.forEach(([name, str]) => console.log(name + ':', c.includes(str) ? 'OK' : 'MISSING - ' + str))"</automated>
  </verify>
  <acceptance_criteria>
    - `grep "from '@google/generative-ai'" lib/gemini.ts` retorna 1 línea
    - `grep "GOOGLE_GEMINI_API_KEY!" lib/gemini.ts` retorna 1 línea
    - `grep "'gemini-2.0-flash'" lib/gemini.ts` retorna 1 línea
    - `grep "export interface CandidateData" lib/gemini.ts` retorna 1 línea
    - `grep "export async function generateEmailBody" lib/gemini.ts` retorna 1 línea
    - `grep "429" lib/gemini.ts` retorna al menos 2 líneas (detección + log)
    - `grep "Math.pow(2, attempt)" lib/gemini.ts` retorna 1 línea
    - `grep "maxAttempts = 3" lib/gemini.ts` retorna 1 línea
    - El prompt en el código contiene "CV se adjunta" (o equivalente — mención explícita del CV adjunto)
    - `npm run build` no introduce nuevos errores de TypeScript
  </acceptance_criteria>
  <done>lib/gemini.ts existe, exporta CandidateData y generateEmailBody(), el prompt incluye todos los elementos de GEN-02, y el retry exponencial detecta 429 con delays 1s/2s/4s por máximo 3 intentos.</done>
</task>

<task type="auto">
  <name>Task 2: lib/gmail.ts — Envío MIME multipart con base64url y access_token OAuth</name>
  <files>lib/gmail.ts</files>
  <read_first>
    - C:\Users\Equipo\Documents\val-thorens\.planning\phases\02-pipeline\02-RESEARCH.md (sección "Pattern 5: Gmail API — Envío con adjunto PDF", líneas 495-557, y "Pitfall 3: base64url vs base64 estándar", líneas 638-650)
    - C:\Users\Equipo\Documents\val-thorens\.planning\phases\02-pipeline\02-PATTERNS.md (sección "lib/gmail.ts", líneas 325-408)
  </read_first>
  <action>
Crear `lib/gmail.ts` con el siguiente contenido exacto:

```typescript
export interface SendEmailParams {
  accessToken: string;
  to: string;
  subject: string;
  body: string;
  cvBase64: string;
  cvFilename: string;
  fromEmail: string;
}

export async function sendEmail(params: SendEmailParams): Promise<void> {
  const boundary = `boundary_${Date.now()}_${Math.random().toString(36).slice(2)}`;

  // SEND-01: construir mensaje MIME RFC 2822 multipart/mixed con body texto y adjunto PDF
  const mimeMessage = [
    `From: ${params.fromEmail}`,
    `To: ${params.to}`,
    `Subject: ${params.subject}`,
    'MIME-Version: 1.0',
    `Content-Type: multipart/mixed; boundary="${boundary}"`,
    '',
    `--${boundary}`,
    'Content-Type: text/plain; charset=UTF-8',
    'Content-Transfer-Encoding: quoted-printable',
    '',
    params.body,
    '',
    `--${boundary}`,
    'Content-Type: application/pdf',
    `Content-Disposition: attachment; filename="${params.cvFilename}"`,
    'Content-Transfer-Encoding: base64',
    '',
    params.cvBase64,
    '',
    `--${boundary}--`,
  ].join('\r\n');

  // CRÍTICO: base64url encoding — 3 reemplazos OBLIGATORIOS (RESEARCH.md Pitfall 3)
  // Gmail API requiere base64url (RFC 4648), NO base64 estándar
  // Sin estos reemplazos: Gmail API devuelve 400 "Invalid base64 encoding"
  const encoded = Buffer.from(mimeMessage)
    .toString('base64')
    .replace(/\+/g, '-')   // reemplazo 1: + → -
    .replace(/\//g, '_')   // reemplazo 2: / → _
    .replace(/=+$/, '');   // reemplazo 3: eliminar padding =

  // SEND-02: enviar via Gmail API usando el access_token OAuth del usuario
  const response = await fetch(
    'https://gmail.googleapis.com/gmail/v1/users/me/messages/send',
    {
      method: 'POST',
      headers: {
        Authorization: `Bearer ${params.accessToken}`,
        'Content-Type': 'application/json',
      },
      body: JSON.stringify({ raw: encoded }),
    }
  );

  if (!response.ok) {
    const errorText = await response.text();
    throw new Error(`Gmail send failed: ${response.status} — ${errorText}`);
    // El caller (run/route.ts) captura este throw, emite SSE event 'send_error', y continúa
  }
}
```

Notas críticas:
- Los 3 reemplazos de base64url son OBLIGATORIOS (ver RESEARCH.md Pitfall 3): `+` → `-`, `/` → `_`, `=` → eliminado
- El `cvBase64` que llega del frontend ya incluye el prefijo `data:application/pdf;base64,` — el caller (run/route.ts) DEBE extraer solo la parte después de la coma: `cvBase64.split(',')[1]`
- El boundary incluye `Math.random()` para que sea único en cada llamada
- `\r\n` como separador de líneas del MIME (RFC 2822 requiere CRLF)
- NO importar googleapis ni ninguna otra librería — la llamada es fetch nativo
- SEND-03: el sleep(4000ms) entre envíos es responsabilidad de run/route.ts, no de esta función
- SEND-04: si el envío falla, esta función lanza un Error; el caller (run/route.ts) captura y continúa
  </action>
  <verify>
    <automated>cd "C:/Users/Equipo/Documents/val-thorens" && node -e "const fs = require('fs'); const c = fs.readFileSync('lib/gmail.ts', 'utf8'); const checks = [['export SendEmailParams', 'export interface SendEmailParams'], ['export sendEmail', 'export async function sendEmail'], ['MIME boundary', 'boundary_'], ['Content-Type multipart', 'multipart/mixed'], ['base64url repl 1', \"replace(/\\\\+/g, '-')\"], ['base64url repl 2', \"replace(/\\\\//g, '_')\"], ['base64url repl 3', \"replace(/=+\$/\"], ['Gmail API endpoint', 'gmail.googleapis.com/gmail/v1/users/me/messages/send'], ['Authorization Bearer', 'Authorization: \`Bearer'], ['raw encoded', \"{ raw: encoded }\"], ['CRLF join', \"join('\\\\r\\\\n')\"]]; checks.forEach(([name, str]) => console.log(name + ':', c.includes(str) ? 'OK' : 'MISSING - ' + str))"</automated>
  </verify>
  <acceptance_criteria>
    - `grep "export interface SendEmailParams" lib/gmail.ts` retorna 1 línea
    - `grep "export async function sendEmail" lib/gmail.ts` retorna 1 línea
    - `grep "multipart/mixed" lib/gmail.ts` retorna 1 línea (header MIME)
    - `grep "replace(/\+/g, '-')" lib/gmail.ts` retorna 1 línea (base64url reemplazo 1)
    - `grep "replace(/\//g, '_')" lib/gmail.ts` retorna 1 línea (base64url reemplazo 2)
    - `grep "replace(/=+\$/" lib/gmail.ts` retorna 1 línea (base64url reemplazo 3)
    - `grep "gmail.googleapis.com/gmail/v1/users/me/messages/send" lib/gmail.ts` retorna 1 línea
    - `grep "Authorization" lib/gmail.ts` retorna al menos 1 línea con "Bearer"
    - `grep "join('\\r\\n')" lib/gmail.ts` retorna 1 línea (CRLF separator)
    - `npm run build` no introduce nuevos errores de TypeScript
  </acceptance_criteria>
  <done>lib/gmail.ts existe, exporta SendEmailParams y sendEmail(), construye MIME multipart/mixed con body texto y PDF adjunto, aplica los 3 reemplazos de base64url, y envía via Gmail API con Authorization: Bearer.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| frontend accessToken → lib/gmail.ts | Token OAuth del usuario viaja en el body del POST — debe usarse solo para gmail.send |
| Gemini API response → email body | Contenido generado por IA; podría contener texto inesperado |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-03-01 | Information Disclosure | lib/gmail.ts — accessToken en logs | mitigate | NUNCA loguear params.accessToken — si hay error, loguear solo el código HTTP y el texto de error, no el token |
| T-02-03-02 | Tampering | lib/gmail.ts — params.to (email destino) | accept | El email destino viene del scraping de un website real; confiamos en los datos de Puppeteer para v1 |
| T-02-03-03 | Information Disclosure | lib/gemini.ts — GOOGLE_GEMINI_API_KEY | mitigate | Key solo usada en servidor (lib/); nunca expuesta al cliente; no loguear el valor |
| T-02-03-04 | Elevation of Privilege | lib/gmail.ts — scope gmail.send | mitigate | El scope ya está limitado a gmail.send en NextAuth (app/api/auth/[...nextauth]/route.ts); lib/gmail.ts solo llama /messages/send — no leer inbox ni hacer otras operaciones |
</threat_model>

<verification>
```bash
# Verificar lib/gemini.ts
grep "from '@google/generative-ai'" lib/gemini.ts
grep "'gemini-2.0-flash'" lib/gemini.ts
grep "export interface CandidateData" lib/gemini.ts
grep "429" lib/gemini.ts
grep "Math.pow(2, attempt)" lib/gemini.ts

# Verificar lib/gmail.ts
grep "multipart/mixed" lib/gmail.ts
grep "gmail.googleapis.com/gmail/v1/users/me/messages/send" lib/gmail.ts
grep "replace" lib/gmail.ts  # debe mostrar los 3 reemplazos base64url

# Build clean
npm run build
```
</verification>

<success_criteria>
- lib/gemini.ts: prompt en francés con todos los elementos de GEN-02, modelo gemini-2.0-flash, retry exponencial (1s/2s/4s, max 3 intentos) solo para 429
- lib/gmail.ts: MIME multipart/mixed con body texto + PDF adjunto, base64url con los 3 reemplazos, fetch a gmail.googleapis.com con Bearer token, throw en error para que el caller continúe el pipeline
- npm run build pasa sin errores
</success_criteria>

<output>
Después de completar este plan, crear `.planning/phases/02-pipeline/02-03-SUMMARY.md` siguiendo el template en `@$HOME/.claude/get-shit-done/templates/summary.md`.
</output>
