---
phase: 02-pipeline
plan: "05"
type: execute
wave: 3
depends_on: ["02-02", "02-03", "02-04"]
files_modified:
  - app/api/run/route.ts
autonomous: true
requirements: [DISC-01, DISC-02, DISC-03, DISC-04, DISC-05, SCRP-01, SCRP-02, SCRP-03, SCRP-04, SCRP-05, GEN-01, GEN-02, GEN-03, GEN-04, SEND-01, SEND-02, SEND-03, SEND-04, SHTS-01, SHTS-02, SHTS-03]

must_haves:
  truths:
    - "POST /api/run retorna un Response con ReadableStream de tipo text/event-stream (SSE)"
    - "El pipeline requiere sesión autenticada — retorna 401 si no hay sesión"
    - "El pipeline emite eventos SSE de tipo: searching, discovery_complete, scraping, generating, sent, send_error, logged, complete"
    - "El asunto del email es: Candidature - Saison d'hiver {año actual} - {nombre del candidato}"
    - "El pipeline espera 4 segundos entre cada envío de email"
    - "Los fallos individuales de envío emiten SSE event 'send_error' y el pipeline continúa con el siguiente empleador"
    - "El evento 'complete' incluye contadores: sent (enviados) y skipped (sin email o fallidos)"
    - "El cvBase64 del body incluye el prefijo data:application/pdf;base64, — el pipeline extrae solo la parte después de la coma"
  artifacts:
    - path: "app/api/run/route.ts"
      provides: "Orquestador SSE del pipeline completo — 4 etapas: discover, scrape, generate, send+log"
      exports: ["POST"]
  key_links:
    - from: "app/api/run/route.ts"
      to: "lib/places.ts — discoverEmployers()"
      via: "import y llamada directa"
      pattern: "discoverEmployers"
    - from: "app/api/run/route.ts"
      to: "lib/scraper.ts — scrapeEmail()"
      via: "import y llamada directa"
      pattern: "scrapeEmail"
    - from: "app/api/run/route.ts"
      to: "lib/gemini.ts — generateEmailBody()"
      via: "import y llamada directa"
      pattern: "generateEmailBody"
    - from: "app/api/run/route.ts"
      to: "lib/gmail.ts — sendEmail()"
      via: "import y llamada directa"
      pattern: "sendEmail"
    - from: "app/api/run/route.ts"
      to: "lib/sheets.ts — logToSheets()"
      via: "import y llamada directa"
      pattern: "logToSheets"
---

<objective>
Implementar el orquestador SSE del pipeline completo en `app/api/run/route.ts`. Este endpoint acepta POST con los datos del formulario, verifica la sesión, y retorna un ReadableStream que emite eventos SSE mientras ejecuta las 4 etapas en secuencia: discovery (Places API), scraping (Puppeteer), generación (Gemini), envío + logging (Gmail + Sheets).

Purpose: Este es el endpoint central del producto — es lo que el formulario ya está llamando desde Fase 1 (fire-and-forget) y lo que Fase 3 conectará para mostrar el progreso en tiempo real.
Output: app/api/run/route.ts exporta POST handler que retorna SSE stream con el pipeline completo.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/02-pipeline/02-RESEARCH.md
@.planning/phases/02-pipeline/02-PATTERNS.md
@.planning/phases/02-pipeline/02-02-SUMMARY.md
@.planning/phases/02-pipeline/02-03-SUMMARY.md
@.planning/phases/02-pipeline/02-04-SUMMARY.md

<interfaces>
<!-- Contratos de todos los lib/* que este plan importa -->

Desde lib/places.ts (02-02):
```typescript
export interface Employer {
  placeId: string;
  name: string;
  address: string;
  website: string | null;
}
export async function discoverEmployers(): Promise<Employer[]>
```

Desde lib/scraper.ts (02-02):
```typescript
export async function scrapeEmail(url: string): Promise<string | null>
```

Desde lib/gemini.ts (02-03):
```typescript
export interface CandidateData {
  name: string;
  jobTypes: string[];
  languages: string;
  availFrom: string;
  availTo: string;
}
export async function generateEmailBody(
  candidate: CandidateData,
  employer: { name: string }
): Promise<string>
```

Desde lib/gmail.ts (02-03):
```typescript
export async function sendEmail(params: {
  accessToken: string;
  to: string;
  subject: string;
  body: string;
  cvBase64: string;
  cvFilename: string;
  fromEmail: string;
}): Promise<void>
```

Desde lib/sheets.ts (02-04):
```typescript
export async function logToSheets(userName: string, employerName: string): Promise<void>
```

Desde app/page.tsx (Fase 1 — lo que el frontend ya envía):
```typescript
// body del POST que llega a este endpoint:
{
  name: string,
  cvBase64: string,           // formato: "data:application/pdf;base64,<base64data>"
  jobTypes: string[],
  languages: string,
  availFrom: string,          // formato: "YYYY-MM-DD"
  availTo: string,            // formato: "YYYY-MM-DD"
  accessToken: string,        // OAuth access_token del usuario
}
```

Desde types/next-auth.d.ts (Fase 1):
```typescript
interface Session {
  access_token?: string;
  user?: { email?: string; name?: string }
}
```

SSE event types que Fase 3 esperará (de PATTERNS.md líneas 631-640):
```typescript
{ type: 'searching', query: string, found: number }
{ type: 'discovery_complete', total: number }
{ type: 'scraping', employer: string, email: string | null }
{ type: 'generating', employer: string }
{ type: 'sent', employer: string, email: string }
{ type: 'send_error', employer: string, error: string }
{ type: 'logged', employer: string }
{ type: 'complete', sent: number, skipped: number }
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: app/api/run/route.ts — Orquestador SSE del pipeline completo</name>
  <files>app/api/run/route.ts</files>
  <read_first>
    - C:\Users\Equipo\Documents\val-thorens\.planning\phases\02-pipeline\02-RESEARCH.md (sección "Pattern 1: SSE con ReadableStream en Next.js App Router", líneas 193-241, y "System Architecture Diagram", líneas 123-163)
    - C:\Users\Equipo\Documents\val-thorens\.planning\phases\02-pipeline\02-PATTERNS.md (sección "app/api/run/route.ts", líneas 27-100)
    - C:\Users\Equipo\Documents\val-thorens\app\api\auth\[...nextauth]\route.ts (verificar que authOptions está exportada — necesaria para getServerSession(authOptions))
  </read_first>
  <action>
Crear `app/api/run/route.ts` con el siguiente contenido exacto:

```typescript
import { getServerSession } from 'next-auth';
import { authOptions } from '@/app/api/auth/[...nextauth]/route';
import { discoverEmployers } from '@/lib/places';
import { scrapeEmail } from '@/lib/scraper';
import { generateEmailBody, CandidateData } from '@/lib/gemini';
import { sendEmail } from '@/lib/gmail';
import { logToSheets } from '@/lib/sheets';

const encoder = new TextEncoder();

function sseEvent(data: object): Uint8Array {
  return encoder.encode(`data: ${JSON.stringify(data)}\n\n`);
}

function sleep(ms: number) {
  return new Promise((resolve) => setTimeout(resolve, ms));
}

export async function POST(request: Request) {
  // ASVS V4: verificar sesión antes de ejecutar el pipeline
  // Pasar authOptions es obligatorio en NextAuth v4 + App Router — sin authOptions
  // getServerSession() puede siempre retornar null (no lee las callbacks de sesión)
  const session = await getServerSession(authOptions);
  if (!session) {
    return new Response(JSON.stringify({ error: 'Unauthorized' }), {
      status: 401,
      headers: { 'Content-Type': 'application/json' },
    });
  }

  // Parsear y validar el body del POST
  let body: {
    name: string;
    cvBase64: string;
    jobTypes: string[];
    languages: string;
    availFrom: string;
    availTo: string;
    accessToken: string;
  };

  try {
    body = await request.json();
  } catch {
    return new Response(JSON.stringify({ error: 'Invalid JSON body' }), {
      status: 400,
      headers: { 'Content-Type': 'application/json' },
    });
  }

  const { name, cvBase64, jobTypes, languages, availFrom, availTo, accessToken } = body;

  // ASVS V5: validar campos requeridos antes del pipeline
  if (!name || !cvBase64 || !accessToken) {
    return new Response(JSON.stringify({ error: 'Missing required fields: name, cvBase64, accessToken' }), {
      status: 400,
      headers: { 'Content-Type': 'application/json' },
    });
  }

  // El cvBase64 del frontend tiene formato "data:application/pdf;base64,<data>"
  // Extraer solo la parte de los datos base64 para el adjunto MIME
  const cvBase64Data = cvBase64.includes(',') ? cvBase64.split(',')[1] : cvBase64;

  // GEN-03: asunto hardcodeado con año actual
  const currentYear = new Date().getFullYear();
  const emailSubject = `Candidature - Saison d'hiver ${currentYear} - ${name}`;

  const fromEmail = session.user?.email ?? '';

  const candidate: CandidateData = {
    name,
    jobTypes: jobTypes ?? [],
    languages: languages ?? '',
    availFrom: availFrom ?? '',
    availTo: availTo ?? '',
  };

  const stream = new ReadableStream({
    async start(controller) {
      let sentCount = 0;
      let skippedCount = 0;

      try {
        // ─── ETAPA 1: DISCOVERY ───────────────────────────────────────────
        controller.enqueue(sseEvent({ type: 'searching', message: 'Buscando empleadores en Val Thorens...' }));

        const employers = await discoverEmployers();

        controller.enqueue(sseEvent({ type: 'discovery_complete', total: employers.length }));

        // ─── ETAPAS 2-4: POR EMPLEADOR ────────────────────────────────────
        for (const employer of employers) {
          try {
            // ETAPA 2: SCRAPING
            // Sin website: emitir scraping con null y saltar
            if (!employer.website) {
              skippedCount++;
              controller.enqueue(sseEvent({ type: 'scraping', employer: employer.name, email: null }));
              continue;
            }

            // Con website: scraping y emitir resultado final (un único evento por empleador)
            const email = await scrapeEmail(employer.website);
            controller.enqueue(sseEvent({ type: 'scraping', employer: employer.name, email: email }));

            if (!email) {
              // SCRP-05: sin email encontrado → marcar como skipped
              skippedCount++;
              continue;
            }

            // ETAPA 3: GENERACIÓN DE EMAIL
            controller.enqueue(sseEvent({ type: 'generating', employer: employer.name }));
            const emailBody = await generateEmailBody(candidate, { name: employer.name });

            // ETAPA 4: ENVÍO
            await sendEmail({
              accessToken,
              to: email,
              subject: emailSubject,
              body: emailBody,
              cvBase64: cvBase64Data,
              cvFilename: 'CV.pdf',
              fromEmail,
            });

            sentCount++;
            controller.enqueue(sseEvent({ type: 'sent', employer: employer.name, email }));

            // SEND-03: esperar 4 segundos entre envíos
            await sleep(4000);

            // LOGGING: registrar en Sheets (graceful — no abortar si falla)
            try {
              await logToSheets(name, employer.name);
              controller.enqueue(sseEvent({ type: 'logged', employer: employer.name }));
            } catch (sheetsErr) {
              // Error de logging es no-bloqueante — el email ya fue enviado
              console.error('[sheets] Log failed (non-blocking):', sheetsErr);
            }

          } catch (employerErr) {
            // SEND-04 / CLAUDE.md: error individual → log + continuar, nunca abortar el pipeline
            const errMessage = employerErr instanceof Error ? employerErr.message : String(employerErr);
            skippedCount++;
            controller.enqueue(sseEvent({ type: 'send_error', employer: employer.name, error: errMessage }));
          }
        }

        // Pipeline completado
        controller.enqueue(sseEvent({ type: 'complete', sent: sentCount, skipped: skippedCount }));

      } catch (fatalErr) {
        // Error fatal (ej: discovery falló completamente) — emitir evento y cerrar
        const errMessage = fatalErr instanceof Error ? fatalErr.message : String(fatalErr);
        controller.enqueue(sseEvent({ type: 'send_error', employer: 'pipeline', error: errMessage }));
        controller.enqueue(sseEvent({ type: 'complete', sent: sentCount, skipped: skippedCount }));
      } finally {
        controller.close();
      }
    },
  });

  return new Response(stream, {
    headers: {
      'Content-Type': 'text/event-stream',
      'Cache-Control': 'no-cache',
      'Connection': 'keep-alive',
    },
  });
}
```

Notas críticas:
- `getServerSession(authOptions)` — OBLIGATORIO pasar authOptions en NextAuth v4 + App Router. Sin authOptions, getServerSession() puede retornar null aunque el usuario esté autenticado (no encuentra las callbacks de sesión). Importar authOptions desde `@/app/api/auth/[...nextauth]/route`.
- Si authOptions no está exportada en ese archivo (exporta solo el handler), añadir `export { authOptions }` al route o extraer authOptions a `lib/auth.ts` e importar desde allí en ambos archivos.
- `cvBase64.split(',')[1]` extrae solo los bytes del PDF del formato data URL del FileReader
- `cvFilename: 'CV.pdf'` — nombre genérico para el adjunto (el nombre original no se necesita para v1)
- El evento `scraping` se emite UNA SOLA VEZ por empleador: en el caso sin website (null inmediato) y en el caso con website (resultado del scraping, sea email o null)
- Orden de eventos por empleador: scraping → generating? → sent/send_error → logged?
- El `sleep(4000)` va DESPUÉS del envío exitoso y ANTES del logging
  </action>
  <verify>
    <automated>cd "C:/Users/Equipo/Documents/val-thorens" && node -e "const fs = require('fs'); const c = fs.readFileSync('app/api/run/route.ts', 'utf8'); const checks = [['import getServerSession', \"from 'next-auth'\"], ['import authOptions', 'authOptions'], ['import discoverEmployers', \"from '@/lib/places'\"], ['import scrapeEmail', \"from '@/lib/scraper'\"], ['import generateEmailBody', \"from '@/lib/gemini'\"], ['import sendEmail', \"from '@/lib/gmail'\"], ['import logToSheets', \"from '@/lib/sheets'\"], ['ReadableStream', 'new ReadableStream'], ['text/event-stream', 'text/event-stream'], ['SSE encoder', 'TextEncoder'], ['auth check 401', 'status: 401'], ['getServerSession with authOptions', 'getServerSession(authOptions)'], ['cvBase64 split', \"cvBase64.split(',')[1]\"], ['email subject', \"Saison d'hiver\"], ['sleep 4000', 'sleep(4000)'], ['event complete', \"type: 'complete'\"], ['event sent', \"type: 'sent'\"], ['event send_error', \"type: 'send_error'\"], ['event discovery_complete', \"type: 'discovery_complete'\"], ['controller.close', 'controller.close()'], ['finally close', 'finally']]; checks.forEach(([name, str]) => console.log(name + ':', c.includes(str) ? 'OK' : 'MISSING - ' + str))"</automated>
  </verify>
  <acceptance_criteria>
    - `grep "from 'next-auth'" app/api/run/route.ts` retorna al menos 1 línea (import getServerSession)
    - `grep "authOptions" app/api/run/route.ts` retorna al menos 2 líneas (import + uso en getServerSession)
    - `grep "getServerSession(authOptions)" app/api/run/route.ts` retorna 1 línea (NextAuth v4 requiere authOptions)
    - `grep "from '@/lib/places'" app/api/run/route.ts` retorna 1 línea
    - `grep "from '@/lib/scraper'" app/api/run/route.ts` retorna 1 línea
    - `grep "from '@/lib/gemini'" app/api/run/route.ts` retorna 1 línea
    - `grep "from '@/lib/gmail'" app/api/run/route.ts` retorna 1 línea
    - `grep "from '@/lib/sheets'" app/api/run/route.ts` retorna 1 línea
    - `grep "text/event-stream" app/api/run/route.ts` retorna 1 línea (Content-Type del response)
    - `grep "status: 401" app/api/run/route.ts` retorna 1 línea (guard de autenticación)
    - `grep "split(',')[1]" app/api/run/route.ts` retorna 1 línea (extracción de base64 del data URL)
    - `grep "Saison d'hiver" app/api/run/route.ts` retorna 1 línea (asunto hardcodeado GEN-03)
    - `grep "sleep(4000)" app/api/run/route.ts` retorna 1 línea (SEND-03: delay entre envíos)
    - `grep "type: 'complete'" app/api/run/route.ts` retorna al menos 1 línea (evento final)
    - `grep "type: 'sent'" app/api/run/route.ts` retorna al menos 1 línea
    - `grep "type: 'send_error'" app/api/run/route.ts` retorna al menos 1 línea
    - `grep "controller.close()" app/api/run/route.ts` retorna al menos 1 línea (en bloque finally)
    - `grep "export async function POST" app/api/run/route.ts` retorna 1 línea
    - `npm run build` no introduce nuevos errores de TypeScript
  </acceptance_criteria>
  <done>app/api/run/route.ts existe, exporta POST handler, retorna ReadableStream SSE, implementa el pipeline completo de 4 etapas con manejo de errores individuales, emite todos los SSE event types requeridos, el pipeline no se aborta por fallos individuales, y getServerSession recibe authOptions correctamente.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| POST request body → pipeline | Datos del formulario + accessToken OAuth del usuario |
| pipeline → Gmail API | accessToken enviado a API de Google para enviar emails |
| pipeline → Gemini API | Datos del candidato enviados a la IA para generar emails |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-05-01 | Elevation of Privilege | app/api/run — ejecución sin autenticación | mitigate | `getServerSession(authOptions)` verifica sesión al inicio; retorna 401 si no hay sesión activa — el pipeline nunca ejecuta para usuarios no autenticados |
| T-02-05-02 | Tampering | app/api/run — body.accessToken desde cliente | accept | El accessToken lo provee el cliente (ya autenticado con NextAuth); solo tiene scope gmail.send — no puede hacer operaciones destructivas |
| T-02-05-03 | Information Disclosure | app/api/run — accessToken en logs | mitigate | NUNCA loguear `accessToken`, `cvBase64`, ni `GOOGLE_*` keys — solo loguear nombres de empleadores y mensajes de error no-sensibles |
| T-02-05-04 | Denial of Service | app/api/run — pipeline de larga duración | accept | `maxDuration: 300` en vercel.json limita a 5 minutos; el pipeline está diseñado para ~8-13 minutos (dentro del límite para el scope típico de Val Thorens) |
| T-02-05-05 | Tampering | app/api/run — cvBase64 malicioso | accept | El CV se adjunta como bytes base64 sin procesamiento ni ejecución; Gmail API lo transmite directamente; el riesgo es mínimo para v1 |
</threat_model>

<verification>
```bash
# Verificar todos los imports
grep "from '@/lib/" app/api/run/route.ts

# Verificar authOptions (WARNING fix — NextAuth v4 + App Router)
grep "getServerSession(authOptions)" app/api/run/route.ts
grep "authOptions" app/api/run/route.ts

# Verificar SSE
grep "text/event-stream" app/api/run/route.ts
grep "ReadableStream" app/api/run/route.ts

# Verificar seguridad
grep "status: 401" app/api/run/route.ts

# Verificar constraints del pipeline
grep "sleep(4000)" app/api/run/route.ts
grep "Saison d'hiver" app/api/run/route.ts
grep "split(',')[1]" app/api/run/route.ts

# Verificar eventos SSE
grep "type: 'complete'" app/api/run/route.ts
grep "type: 'sent'" app/api/run/route.ts
grep "type: 'send_error'" app/api/run/route.ts

# Build final de toda la fase
npm run build
```
</verification>

<success_criteria>
- app/api/run/route.ts: POST handler con ReadableStream SSE, guard de autenticación 401 con getServerSession(authOptions)
- authOptions importado desde `@/app/api/auth/[...nextauth]/route` y pasado a getServerSession
- Extrae cvBase64Data correctamente con split(',')[1]
- Asunto hardcodeado: "Candidature - Saison d'hiver {año} - {nombre}"
- Sleep de 4000ms entre envíos
- Evento scraping emitido UNA SOLA VEZ por empleador con el resultado final (email o null)
- Todos los SSE event types emitidos: searching, discovery_complete, scraping, generating, sent, send_error, logged, complete
- Errores individuales por empleador emiten send_error y continúan — nunca abortan el pipeline
- El logging de Sheets falla gracefully (no bloqueante)
- npm run build pasa sin errores — fase 2 completa y funcional
</success_criteria>

<output>
Después de completar este plan, crear `.planning/phases/02-pipeline/02-05-SUMMARY.md` siguiendo el template en `@$HOME/.claude/get-shit-done/templates/summary.md`.
</output>
