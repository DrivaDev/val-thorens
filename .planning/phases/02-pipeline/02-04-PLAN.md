---
phase: 02-pipeline
plan: "04"
type: execute
wave: 2
depends_on: ["02-01", "02-02"]
files_modified:
  - lib/sheets.ts
  - app/api/scrape/route.ts
autonomous: true
requirements: [SHTS-01, SHTS-02, SHTS-03]

must_haves:
  truths:
    - "logToSheets() autentica con Service Account JSON desde env var y hace append de una fila [userName, employerName, 'No'] al Google Sheet hardcodeado"
    - "app/api/scrape/route.ts acepta POST con { url } y retorna { email } o { email: null } usando scrapeEmail() de lib/scraper.ts"
    - "El Google Sheet ID está hardcodeado como 1Sq8Uy0SdeMrbIxHbUAcZ4Dsc1K2QufeZify4pL59Dek"
  artifacts:
    - path: "lib/sheets.ts"
      provides: "Función logToSheets() con auth via Service Account"
      exports: ["logToSheets"]
    - path: "app/api/scrape/route.ts"
      provides: "Endpoint POST que envuelve scrapeEmail() con timeout propio (maxDuration: 60)"
      exports: ["POST"]
  key_links:
    - from: "lib/sheets.ts"
      to: "sheets.googleapis.com"
      via: "googleapis — sheets.spreadsheets.values.append()"
      pattern: "spreadsheets\\.values\\.append"
    - from: "app/api/scrape/route.ts"
      to: "lib/scraper.ts — scrapeEmail()"
      via: "import scrapeEmail y llamada directa"
      pattern: "scrapeEmail"
---

<objective>
Implementar el módulo de logging a Sheets y el endpoint de scraping: `lib/sheets.ts` que autentica con Service Account y hace append al Google Sheet, y `app/api/scrape/route.ts` que envuelve scrapeEmail() como endpoint separado con su propio timeout de 60s (ya configurado en vercel.json desde Fase 1).

Purpose: Sheets completa el ciclo del pipeline (discovery → scrape → generate → send → log). El endpoint scrape/route.ts existe porque vercel.json ya tiene maxDuration: 60 para él — tener un endpoint separado permite que el scraping tenga su propio límite de tiempo independiente del pipeline principal.
Output: lib/sheets.ts exporta logToSheets(); app/api/scrape/route.ts exporta POST handler.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/02-pipeline/02-RESEARCH.md
@.planning/phases/02-pipeline/02-PATTERNS.md
@.planning/phases/02-pipeline/02-01-SUMMARY.md
@.planning/phases/02-pipeline/02-02-SUMMARY.md

<interfaces>
<!-- Contratos que este plan define -->

Contrato de logToSheets que run/route.ts (Wave 3) consumirá:
```typescript
export async function logToSheets(userName: string, employerName: string): Promise<void>
```

Contrato del endpoint scrape/route.ts que run/route.ts (Wave 3) puede llamar opcionalmente:
```typescript
// POST /api/scrape
// Request body: { url: string }
// Response: { email: string | null }
// Error response: { error: string } con status 500
```

Desde vercel.json (ya configurado en Fase 1):
```json
{
  "functions": {
    "app/api/run/route.ts": { "maxDuration": 300 },
    "app/api/scrape/route.ts": { "maxDuration": 60 }
  }
}
```

Desde lib/scraper.ts (creado en plan 02-02):
```typescript
export async function scrapeEmail(url: string): Promise<string | null>
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: lib/sheets.ts — Logging a Google Sheets con Service Account</name>
  <files>lib/sheets.ts</files>
  <read_first>
    - C:\Users\Equipo\Documents\val-thorens\.planning\phases\02-pipeline\02-RESEARCH.md (sección "Pattern 6: Google Sheets API — Append con Service Account", líneas 563-594)
    - C:\Users\Equipo\Documents\val-thorens\.planning\phases\02-pipeline\02-PATTERNS.md (sección "lib/sheets.ts", líneas 411-466)
  </read_first>
  <action>
Crear `lib/sheets.ts` con el siguiente contenido exacto:

```typescript
import { google } from 'googleapis';

// SHTS-01: Sheet ID hardcodeado (de CLAUDE.md)
const SHEET_ID = '1Sq8Uy0SdeMrbIxHbUAcZ4Dsc1K2QufeZify4pL59Dek';

export async function logToSheets(userName: string, employerName: string): Promise<void> {
  // SHTS-03: autenticar via Service Account JSON desde env var
  const credentials = JSON.parse(process.env.GOOGLE_SERVICE_ACCOUNT_JSON!);

  const auth = new google.auth.GoogleAuth({
    credentials,
    scopes: ['https://www.googleapis.com/auth/spreadsheets'],
  });

  const sheets = google.sheets({ version: 'v4', auth });

  // SHTS-02: columnas A=userName, B=employerName, C="No" (respondido)
  await sheets.spreadsheets.values.append({
    spreadsheetId: SHEET_ID,
    range: 'Sheet1!A:C',
    valueInputOption: 'USER_ENTERED',
    requestBody: {
      values: [[userName, employerName, 'No']],
    },
  });
}
```

Notas críticas:
- SHTS-01: El Sheet ID `1Sq8Uy0SdeMrbIxHbUAcZ4Dsc1K2QufeZify4pL59Dek` es literal hardcodeado (de CLAUDE.md)
- SHTS-03: `JSON.parse(process.env.GOOGLE_SERVICE_ACCOUNT_JSON!)` parsea el JSON de la service account. Si la env var no está configurada o tiene formato incorrecto, esta función lanzará un error — el caller (run/route.ts) debe capturarlo gracefully sin abortar el pipeline
- `range: 'Sheet1!A:C'` — nombre de hoja por defecto; si la hoja del usuario tiene otro nombre, ajustar
- `valueInputOption: 'USER_ENTERED'` permite que Sheets interprete fechas/números automáticamente
- La autenticación usa el scope mínimo necesario: solo `spreadsheets` (no `spreadsheets.readonly`)
- El manejo graceful del error de sheets es responsabilidad de run/route.ts (el pipeline debe continuar aunque el logging falle)
  </action>
  <verify>
    <automated>cd "C:/Users/Equipo/Documents/val-thorens" && node -e "const fs = require('fs'); const c = fs.readFileSync('lib/sheets.ts', 'utf8'); const checks = [['import googleapis', \"from 'googleapis'\"], ['SHEET_ID hardcoded', '1Sq8Uy0SdeMrbIxHbUAcZ4Dsc1K2QufeZify4pL59Dek'], ['GOOGLE_SERVICE_ACCOUNT_JSON', 'GOOGLE_SERVICE_ACCOUNT_JSON!'], ['google.auth.GoogleAuth', 'google.auth.GoogleAuth'], ['spreadsheets scope', 'spreadsheets\\''], ['spreadsheets.values.append', 'spreadsheets.values.append'], ['range Sheet1', 'Sheet1!A:C'], ['values No', \"'No'\"], ['export logToSheets', 'export async function logToSheets']]; checks.forEach(([name, str]) => console.log(name + ':', c.includes(str) ? 'OK' : 'MISSING - ' + str))"</automated>
  </verify>
  <acceptance_criteria>
    - `grep "from 'googleapis'" lib/sheets.ts` retorna 1 línea
    - `grep "1Sq8Uy0SdeMrbIxHbUAcZ4Dsc1K2QufeZify4pL59Dek" lib/sheets.ts` retorna 1 línea (Sheet ID hardcodeado)
    - `grep "GOOGLE_SERVICE_ACCOUNT_JSON!" lib/sheets.ts` retorna 1 línea
    - `grep "google.auth.GoogleAuth" lib/sheets.ts` retorna 1 línea
    - `grep "spreadsheets.values.append" lib/sheets.ts` retorna 1 línea
    - `grep "Sheet1!A:C" lib/sheets.ts` retorna 1 línea
    - `grep "'No'" lib/sheets.ts` retorna al menos 1 línea (valor columna C)
    - `grep "export async function logToSheets" lib/sheets.ts` retorna 1 línea
    - `npm run build` no introduce nuevos errores de TypeScript
  </acceptance_criteria>
  <done>lib/sheets.ts existe, exporta logToSheets(), autentica con Service Account JSON, y hace append de [userName, employerName, 'No'] al Sheet ID hardcodeado.</done>
</task>

<task type="auto">
  <name>Task 2: app/api/scrape/route.ts — Endpoint POST que envuelve scrapeEmail() con timeout de 60s</name>
  <files>app/api/scrape/route.ts</files>
  <read_first>
    - C:\Users\Equipo\Documents\val-thorens\.planning\phases\02-pipeline\02-PATTERNS.md (sección "app/api/scrape/route.ts", líneas 488-512)
    - C:\Users\Equipo\Documents\val-thorens\vercel.json (confirmar que maxDuration: 60 ya está configurado para este endpoint)
    - C:\Users\Equipo\Documents\val-thorens\app\api\auth\[...nextauth]\route.ts (patrón de route handler en App Router)
  </read_first>
  <action>
Crear `app/api/scrape/route.ts` con el siguiente contenido exacto:

```typescript
import { scrapeEmail } from '@/lib/scraper';

export async function POST(request: Request) {
  let url: string;

  try {
    const body = await request.json();
    url = body?.url;
  } catch {
    return new Response(JSON.stringify({ error: 'Invalid JSON body' }), {
      status: 400,
      headers: { 'Content-Type': 'application/json' },
    });
  }

  if (!url || typeof url !== 'string') {
    return new Response(JSON.stringify({ error: 'Missing or invalid url parameter' }), {
      status: 400,
      headers: { 'Content-Type': 'application/json' },
    });
  }

  // Validar esquema URL para prevenir SSRF (RESEARCH.md Security Domain T-SSRF)
  try {
    const parsed = new URL(url);
    if (parsed.protocol !== 'http:' && parsed.protocol !== 'https:') {
      throw new Error('Invalid protocol');
    }
  } catch {
    return new Response(JSON.stringify({ error: 'Invalid URL — must be http or https' }), {
      status: 400,
      headers: { 'Content-Type': 'application/json' },
    });
  }

  try {
    const email = await scrapeEmail(url);
    return new Response(JSON.stringify({ email }), {
      headers: { 'Content-Type': 'application/json' },
    });
  } catch (err) {
    const message = err instanceof Error ? err.message : String(err);
    return new Response(JSON.stringify({ error: message }), {
      status: 500,
      headers: { 'Content-Type': 'application/json' },
    });
  }
}
```

Notas:
- El `maxDuration: 60` para este endpoint ya está configurado en `vercel.json` desde Fase 1 — no necesita ningún cambio
- El endpoint solo exporta `POST` (no GET) — las URL se envían en el body, no en query params
- Validación de URL con `new URL(url)` + comprobación de protocolo http/https previene SSRF básico
- Si `scrapeEmail()` lanza un error (Puppeteer timeout, sitio caído, etc.), retorna 500 con el mensaje; el caller (run/route.ts) captura esto y continúa
- El alias `@/lib/scraper` usa el path alias configurado en tsconfig.json del proyecto
  </action>
  <verify>
    <automated>cd "C:/Users/Equipo/Documents/val-thorens" && node -e "const fs = require('fs'); const c = fs.readFileSync('app/api/scrape/route.ts', 'utf8'); const checks = [['import scrapeEmail', \"from '@/lib/scraper'\"], ['export POST', 'export async function POST'], ['url validation', 'typeof url'], ['protocol check', \"parsed.protocol !== 'http:'\"], ['scrapeEmail call', 'scrapeEmail(url)'], ['JSON response email', '{ email }'], ['Content-Type json', 'application/json'], ['status 400 missing', 'status: 400'], ['status 500 error', 'status: 500']]; checks.forEach(([name, str]) => console.log(name + ':', c.includes(str) ? 'OK' : 'MISSING - ' + str))"</automated>
  </verify>
  <acceptance_criteria>
    - `grep "from '@/lib/scraper'" app/api/scrape/route.ts` retorna 1 línea
    - `grep "export async function POST" app/api/scrape/route.ts` retorna 1 línea
    - `grep "parsed.protocol" app/api/scrape/route.ts` retorna al menos 1 línea (validación protocolo)
    - `grep "scrapeEmail(url)" app/api/scrape/route.ts` retorna 1 línea
    - `grep "application/json" app/api/scrape/route.ts` retorna al menos 2 líneas (Content-Type en responses)
    - `grep "status: 400" app/api/scrape/route.ts` retorna al menos 2 líneas (errores de validación)
    - `grep "status: 500" app/api/scrape/route.ts` retorna 1 línea (error de scraping)
    - `npm run build` no introduce nuevos errores de TypeScript
  </acceptance_criteria>
  <done>app/api/scrape/route.ts existe, exporta POST handler, valida el body y la URL (http/https only), llama scrapeEmail(), retorna { email } o { error } con códigos HTTP apropiados.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GOOGLE_SERVICE_ACCOUNT_JSON env var → JSON.parse() | Credenciales de Service Account; el JSON debe estar bien formado |
| POST /api/scrape body.url → scrapeEmail() | URL externa enviada por el servidor de pipeline; puede ser arbitraria |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-04-01 | Information Disclosure | lib/sheets.ts — GOOGLE_SERVICE_ACCOUNT_JSON | mitigate | No loguear el contenido de la env var; si JSON.parse falla, loguear solo el mensaje de error sin el valor de la variable |
| T-02-04-02 | Tampering | app/api/scrape — SSRF via URL arbitraria | mitigate | Validar que url comienza con http: o https: usando `new URL()` + comprobación de protocol antes de llamar scrapeEmail() |
| T-02-04-03 | Elevation of Privilege | app/api/scrape — endpoint público sin auth | accept | El endpoint es interno al pipeline (llamado desde run/route.ts); para v1 con 5 usuarios en testing mode, el riesgo es bajo. Si se expone públicamente, añadir auth check en v2 |
| T-02-04-04 | Denial of Service | lib/sheets.ts — Service Account con acceso a múltiples sheets | accept | La Service Account solo tiene acceso al Sheet específico compartido con ella; no puede acceder a otros sheets del usuario |
</threat_model>

<verification>
```bash
# Verificar lib/sheets.ts
grep "1Sq8Uy0SdeMrbIxHbUAcZ4Dsc1K2QufeZify4pL59Dek" lib/sheets.ts
grep "GOOGLE_SERVICE_ACCOUNT_JSON!" lib/sheets.ts
grep "spreadsheets.values.append" lib/sheets.ts
grep "export async function logToSheets" lib/sheets.ts

# Verificar app/api/scrape/route.ts
grep "export async function POST" app/api/scrape/route.ts
grep "parsed.protocol" app/api/scrape/route.ts
grep "scrapeEmail(url)" app/api/scrape/route.ts

# Confirmar que vercel.json ya tiene maxDuration: 60 para scrape
grep "60" vercel.json

# Build clean
npm run build
```
</verification>

<success_criteria>
- lib/sheets.ts: autentica con Service Account JSON, Sheet ID hardcodeado correcto, append de [userName, employerName, 'No'] en Sheet1!A:C
- app/api/scrape/route.ts: valida URL (http/https), llama scrapeEmail(), retorna { email } con 200 o { error } con 500
- vercel.json ya tiene maxDuration: 60 para scrape/route.ts (no necesita cambio)
- npm run build pasa sin errores
</success_criteria>

<output>
Después de completar este plan, crear `.planning/phases/02-pipeline/02-04-SUMMARY.md` siguiendo el template en `@$HOME/.claude/get-shit-done/templates/summary.md`.
</output>
